import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  burgerName: {
    color: 'white',
    fontWeight: 'bold',
    fontSize: 16,
    marginBottom: 3,
  },
  textPrice: {
    color: 'green',
    fontWeight: 'bold',
  },
});

export default styles;
